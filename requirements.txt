Django==4.2.7
# mysqlclient==2.2.0  # Install separately when MySQL is set up
# Pillow==10.0.1  # Install separately if needed for image processing
django-crispy-forms==2.1
crispy-bootstrap5==0.7
python-decouple==3.8
# weasyprint==60.2  # Optional: Install separately for PDF generation
# Note: WeasyPrint requires additional system dependencies on Windows
# For Windows: Install GTK+ runtime from https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer
# For Linux: sudo apt-get install build-essential python3-dev python3-pip python3-setuptools python3-wheel python3-cffi libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info 